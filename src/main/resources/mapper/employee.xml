<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.miniproject.domain.employee.EmployeeDao">

	<insert id="insert">
		INSERT INTO employee(NAME, created_at) VALUES(#{name}, NOW())
	</insert>

	<select id="findAll"
		resultType="site.metacoding.miniproject.domain.employee.Employee">
	</select>

	<select id="findById"
		resultType="site.metacoding.miniproject.domain.employee.Employee">

	</select>

	<update id="update">

	</update>

	<delete id="deleteById">

	</delete>


</mapper>