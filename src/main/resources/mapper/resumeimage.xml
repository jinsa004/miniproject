<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.miniproject.domain.image.resume.ResumeImageDao">


    <insert id="insert" useGeneratedKeys="true" keyProperty="resumeImageId">
        INSERT INTO image(origin_image_name, new_image_name, image_path)
		VALUES(#{originImageName}, #{newImageName}, #{imagePath})
    </insert>

</mapper>